services:
  claude-code-python:
    build:
      args:
        VERSION_CLAUDE_CODE: ${VERSION_CLAUDE_CODE:?err}
        VERSION_GIT_SECRETS: ${VERSION_GIT_SECRETS:?err}
        VERSION_GITLEAKS: ${VERSION_GITLEAKS:?err}
        DOCKER_IMAGE_TAG_UV: ${DOCKER_IMAGE_TAG_UV:?err}
      context: .
    image: futureys/claude-code-python:development
    user: root
    volumes:
      - .:/workspace
      - /workspace/.venv
      # - Using uv in Docker | uv
      #   https://docs.astral.sh/uv/guides/integration/docker/#caching
      - uv-cache:/root/.cache/uv
volumes:
  uv-cache: